// Stub — replaced with the full game in Stage 6.
export default function Snake() {
  return (
    <div
      style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        height: '100%',
        fontFamily: 'var(--font-chicago)',
        fontSize: 11,
        minHeight: 80,
      }}
    >
      Loading game… (Stage 6)
    </div>
  );
}
